<h1>Welcome <%=@user.username%></h1>

<p>The following galleries sent you an invite to join their workspace,<br>
but one user can only join one gallery. Please choose one to be your gallery.<br>
The rest of the invitations will be deleted.

<form method="POST" action="/user/gallery/signup">
<%@invites.each do |invite|%>
    <p>
    <label>
    <input type="radio" name="gallery_id" value="<%= invite.user.gallery.id %>"></input>
    <span><%= invite.user.gallery.name %></span>
    </label>
    </p>
<%end%>
  <button class="btn waves-effect waves-light" type="submit" name="action">Sign up
  <i class="material-icons right">send</i>
  </button>
<form>